export interface Inmueble {
    inm_codigo: string;
    cat_nombre: string;
    selected?: boolean;
  }
  
  export interface Tecnologico {
    tec_codigo: string;
    cat_nombre: string;
    selected?: boolean;
  }